A deterministic network supplies an ambient process $X^\theta$. A source law $\mu_0$ induces a state-decorated process $\widetilde X^{\theta,\mu_0}$. At a represented cut, the analysis declares receiver maps $Q_j$ and a phenomenon $\Phi$. Operationally, $Q_K(X)$ is the internal representation made available to the analysis, while a decoder $h$ specifies how that representation is used to predict or realize the target phenomenon.
